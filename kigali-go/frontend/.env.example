# API Configuration
# Copy this file to .env.local and update the values based on your environment

# =============================================================================
# FOR LOCAL DEVELOPMENT ONLY
# =============================================================================

# Option 1: Connect to Vercel-deployed backend (recommended for local dev)
REACT_APP_API_URL=https://go-kigali.vercel.app

# Option 2: Connect to local backend
# REACT_APP_API_URL=http://localhost:5000

# =============================================================================
# FOR VERCEL DEPLOYMENT
# =============================================================================
# In Vercel environment variables, DO NOT SET REACT_APP_API_URL
# or set it to empty string:
# REACT_APP_API_URL=
#
# This makes the frontend use relative URLs (/api/...) which Vercel
# automatically routes to the backend in the same project.
